name: Merge Submodule Matrix + Base PR (on /merge comment)

on:
  issue_comment:
    types: [created]

permissions:
  issues: write
  pull-requests: write
  contents: write

env:
  RW_PAT: ${{ secrets.WORKFLOWS }}

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  init:
    if: |
      github.event.issue.pull_request != null &&
      github.event.comment.body == '/merge' &&
      github.event.comment.user.type != 'Bot'
    runs-on: ubuntu-latest
    outputs:
      head_ref: ${{ steps.pr.outputs.head_ref }}
      base_ref: ${{ steps.pr.outputs.base_ref }}
      pr_number: ${{ steps.pr.outputs.pr_number }}
      submodules: ${{ steps.submod.outputs.submodules }}
    steps:
      - name: Comment start
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = context.payload.comment.id;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
            });
            const newBody = comment.body + "\n" + "🔧 **Merge workflow started**\n\n_This may take a few minutes_";
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
              body: newBody,
            });

      - name: Get PR details
        id: pr
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.issue.number;
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
            });
            core.setOutput("head_ref", pr.data.head.ref);
            core.setOutput("base_ref", pr.data.base.ref);
            core.setOutput("pr_number", pr.data.number);

      - name: Detect submodules (cached)
        id: submod
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.issue.number;
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber
            });
            const cache = comments.find(c => /<!-- SUBMODULES_JSON -->/.test(c.body));
            if (!cache) {
              core.setOutput('submodules', '[]');
            } else {
              const jsonMatch = cache.body.match(/```json\s*([\s\S]*?)\s*```/);
              if (!jsonMatch) throw new Error("Failed to parse JSON from cached comment");
              core.setOutput('submodules', jsonMatch[1]);
            }
      - name: Comment after init
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = context.payload.comment.id;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
            });
            const newBody = comment.body + "\n" + "Submodule information found in comment\nBeginning to check for submodule conflicts";
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
              body: newBody,
            });
      - name: Comment after init on Failure
        if: ${{ failure() }}
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = context.payload.comment.id;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
            });
            const newBody = comment.body + "\n" + "❌ FAILED to get submodule information from base pr comment";
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
              body: newBody,
            });

  check-submodule-conflicts:
    needs: [init]  
    if: ${{ needs.init.outputs.submodules != '[]' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        submod: ${{ fromJson(needs.init.outputs.submodules) }}
      max-parallel: 8
      fail-fast: true
    env:
      GITHUB_API_URL: https://github.com/api/v3
    steps:
      - name: Verify submodule PR state
        id: api-check
        uses: actions/github-script@v7
        env:
          SUBMODULE: ${{ toJson(matrix.submod) }}
        with:
          github-token: ${{ env.RW_PAT }}
          script: |
            const sub = JSON.parse(process.env.SUBMODULE);

            // If there is no PR number attached to this submodule we simply skip it
            if (!sub.pr_number) {
              core.info(`⚙️ Submodule "${sub.name}" has no associated PR – skipping check.`);
              core.setOutput('result', JSON.stringify({path: sub.path, repo: sub.url, result: "no_pr"}));
              return;
            }

            // Extract owner/repo from the submodule URL (handles optional .git suffix)
            const url = new URL(sub.url);
            const [owner, repo] = url.pathname.replace(/^\/|\.git$/g, '').split('/');

            core.info(`🔎 Checking PR #${sub.pr_number} in ${owner}/${repo}`);

            try {
              const { data: prInfo } = await github.rest.pulls.get({
                owner,
                repo,
                pull_number: sub.pr_number,
            });

              if (prInfo.state !== "open") {
                core.setFailed(`❌ Submodule PR #${sub.pr_number} is not open (state=${prInfo.state}).`);
              } else if (!prInfo.mergeable) {
                core.setFailed(`❌ Submodule PR #${sub.pr_number} is not mergeable (has conflicts).`);
            } else {
              core.info(`✅ Submodule PR #${sub.pr_number} is open and mergeable.`);
                core.setOutput('result', JSON.stringify({
                  path: sub.path,
                  repo: `${owner}/${repo}`,
                  result: "mergeable",
                }));
              }
            } catch (err) {
              core.setFailed(`❌ Failed to fetch PR #${sub.pr_number} for ${owner}/${repo}: ${err.message}`);
            }
      - name: Report job failure to PR comment
        if: ${{ failure() }}
        uses: actions/github-script@v7
        env:
          COMMENT_ID: ${{ github.event.comment.id }}
          JOB_NAME:   ${{ github.job }}
          SUBMODULE: ${{ toJson(matrix.submod) }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = Number(process.env.COMMENT_ID);
            const job       = process.env.JOB_NAME;

            // Parse the submodule that was being processed when the job failed
            let subInfo = {};
            try {
              subInfo = JSON.parse(process.env.SUBMODULE);
            } catch (_) {}

            const subDesc = subInfo.name
              ? `**${subInfo.name}** (repo: ${subInfo.url || 'unknown'})`
              : "`unknown submodule`";

            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: commentId
            });

            const failureMsg = `❌ **Job \`${job}\` failed while checking submodule ${subDesc}**\n\nThe PR is either not not open, not approved, or has merge conflicts. Please go check ALL submodules and fix the isue.`;
            const newBody = comment.body + "\n" + failureMsg;

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: commentId,
              body: newBody
            });


  aggregate-precheck:
    needs: check-submodule-conflicts
    runs-on: ubuntu-latest
    steps:
      - name: Comment after Aggregate submod precheck
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = context.payload.comment.id;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
            });
            const newBody = comment.body + "\n" + "No merge conflicts were found in submodule prs. \n Beginning to merge submodules";
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
              body: newBody,
            });

  process-submodules:
    needs: [aggregate-precheck, init]
    if: ${{ needs.init.outputs.submodules != '[]' }}
    runs-on: ubuntu-latest
    env:
      GITHUB_API_URL: https://github.com/api/v3
    strategy:
      matrix:
        submod: ${{ fromJson(needs.init.outputs.submodules) }}
      max-parallel: 4
      fail-fast: true
    steps:
      - name: Merge submodule PR
        uses: actions/github-script@v7
        env:
          COMMENT_ID: ${{ github.event.comment.id }}
        with:
          github-token: ${{ env.RW_PAT }}
          script: |
            const sub = ${{ toJson(matrix.submod) }};
            console.info(`🔎 Processing submodule: ${JSON.stringify(sub)}`);

            const url = new URL(sub.url);
            const [owner, repo] = url.pathname.replace(/^\//,'').replace(/\.git$/,'').split('/');
            const pr = sub.pr_number;
            const { data: prInfo } = await github.rest.pulls.get({ owner, repo, pull_number: pr });
            const res = await github.rest.pulls.merge({
              owner,
              repo,
              pull_number: pr,
              merge_method: "squash",
            });
      - name: Report job failure to PR comment
        if: ${{ failure() }}
        uses: actions/github-script@v7
        env:
          COMMENT_ID: ${{ github.event.comment.id }}
          JOB_NAME: ${{ github.job }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = Number(process.env.COMMENT_ID);
            const job = process.env.JOB_NAME;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: commentId
            });

            const failureMsg = `❌ **Job \`${job}\` failed**`;
            const newBody = comment.body + "\n" + failureMsg;

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: commentId,
              body: newBody
            });

  aggregate:
    needs: process-submodules
    runs-on: ubuntu-latest
    steps:
      - name: Comment after Aggregate 
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = context.payload.comment.id;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
            });
            const newBody = comment.body + "\n" + "All submodules merged.\n Beginning to bump submodules on base Pr branch.";
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
              body: newBody,
            });

  bump-submodules:
    needs: [aggregate, init]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          token: ${{ env.RW_PAT }}
          fetch-depth: 0
          ref: ${{ needs.init.outputs.head_ref }}

      - name: Authenticate submodules
        run: |
          git config --global url."https://${{ env.RW_PAT }}:x-oauth-basic@github.com/".insteadOf "https://github.com/"
          git submodule sync --recursive
          git submodule update --init --recursive

      - name: Bump submodules
        uses: actions/github-script@v7
        env:
          SUBMODULES: ${{ needs.init.outputs.submodules }}
          TOKEN: ${{ env.RW_PAT }}
          BRANCH: ${{ needs.init.outputs.head_ref }}
        with:
          script: |
            const { execSync } = require('child_process');
            const submoduleJson = JSON.parse(process.env.SUBMODULES);
            let changed = false;

            for (const sub of submoduleJson) {
              console.log(`🔧 Updating submodule ${sub.repo} at ${sub.path}`);
              let urlWithToken = sub.url.startsWith('https://')
                ? sub.url.replace('https://', `https://${process.env.TOKEN}@`)
                : sub.url;

              // Get default branch of submodule
              const defaultBranch = execSync(
                `git ls-remote --symref ${urlWithToken} HEAD | grep '^ref:' | awk '{print $2}' | sed 's|refs/heads/||'`,
                { encoding: 'utf8' }
              ).trim();
              
              execSync('git config user.name "github-actions[bot]"');
              execSync('git config user.email "github-actions[bot]@users.noreply.github.com"');

              execSync(`git -C ${sub.path} fetch origin ${defaultBranch}:${defaultBranch}`, { stdio: 'inherit' });
              const latestCommit = execSync(
                `git ls-remote ${urlWithToken} refs/heads/${defaultBranch} | awk '{print $1}'`,
                { encoding: 'utf8' }
              ).trim();
              const currentCommit = execSync(`git -C ${sub.path} rev-parse HEAD`, { encoding: 'utf8' }).trim();

              if (currentCommit !== latestCommit) {
                console.log(`⚠️  ${sub.repo} is outdated. Resetting to ${latestCommit}`);
                execSync(`git -C ${sub.path} reset --hard ${latestCommit}`, { stdio: 'inherit' });
                execSync(`git add ${sub.path}`);
                changed = true;
              } else {
                console.log(`✅ ${sub.repo} is already up-to-date`);
              }
            }
            if (changed) {
              execSync(`git commit -m "Bump submodules to latest default branch"`);
              execSync(`git push origin ${process.env.BRANCH}`);
            } else {
              console.log('✅ All submodules already up-to-date.');
            }
      - name: Comment after Bump
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = context.payload.comment.id;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
            });
            const newBody = comment.body + "\n" + "🔀 Submodules bumped (if needed).\n\n Attempting to resolve any submod merge conflicts if needed.\nBeginning to rebase base pr onto main (resolving submodule pointer conflicts.\n";
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: commentId,
              body: newBody,
            });
      
      
      - name: Start rebase onto base branch
        run: |
          set -e
          git checkout ${{ needs.init.outputs.head_ref }}
          echo "Starting rebase onto ${{needs.init.outputs.base_ref }}..."
          git rebase origin/${{needs.init.outputs.base_ref }} || echo "⚠️ Rebase paused due to conflicts"

      - name: Detect conflicted submodules
        id: detect
        run: |
          conflicted=$(git diff --name-only --diff-filter=U)
          submodule_paths=$(git config --file .gitmodules --get-regexp path | awk '{print $2}')
          conflicted_submodules=""
          for sub in $submodule_paths; do
            if echo "$conflicted" | grep -q "^$sub$"; then
              conflicted_submodules="$conflicted_submodules $sub"
            fi
          done
          echo "Conflicted submodules:$conflicted_submodules"
          echo "submodules=$conflicted_submodules" >> $GITHUB_OUTPUT

      - name: Resolve conflicted submodules
        run: |
          for sub in ${{ steps.detect.outputs.submodules }}; do
            echo "Resolving submodule conflict: $sub"
            # Keep PR branch version ("ours") for the submodule
            git add "$sub"
          done
          if ! git diff --cached --quiet; then
            git commit --no-edit
          fi

      - name: Continue rebase
        run: |
          git rebase --continue || echo "⚠️ Rebase still has conflicts (non-submodule files)"

      - name: Force push rebased branch
        run: git push --force-with-lease origin ${{ needs.init.outputs.head_ref }}

      - name: Report job failure to PR comment
        if: ${{ failure() }}
        uses: actions/github-script@v7
        env:
          COMMENT_ID: ${{ github.event.comment.id }}
          JOB_NAME: ${{ github.job }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = Number(process.env.COMMENT_ID);
            const job = process.env.JOB_NAME;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: commentId
            });

            const failureMsg = `❌ **Job \`${job}\` failed**`;
            const newBody = comment.body + "\n" + failureMsg;

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: commentId,
              body: newBody
            });

  merge-base-no-submodules:
    needs: init
    if: ${{ needs.init.outputs.submodules == '[]' }}
    runs-on: ubuntu-latest
    steps:
      - name: Merge base PR (no submodules)
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.issue.number;
            await github.rest.pulls.merge({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
              merge_method: 'squash'
            });
  merge-base:
    needs: [bump-submodules]
    runs-on: ubuntu-latest
    steps:
      - name: Merge base PR
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.issue.number;
            const comment_id = Number("${{ github.event.comment.id }}");
            await github.rest.pulls.merge({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
              merge_method: 'squash'
            });
            const comment = await github.rest.issues.getComment({ owner: context.repo.owner, repo: context.repo.repo, comment_id });
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id,
              body: comment.data.body.replace("<!-- WORKFLOW_COMMENT -->", "✅ Base PR merged successfully.\n\n<!-- WORKFLOW_COMMENT -->")
            });
      - name: Report job failure to PR comment
        if: ${{ failure() }}
        uses: actions/github-script@v7
        env:
          COMMENT_ID: ${{ github.event.comment.id }}
          JOB_NAME: ${{ github.job }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentId = Number(process.env.COMMENT_ID);
            const job = process.env.JOB_NAME;
            const { data: comment } = await github.rest.issues.getComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: commentId
            });

            const failureMsg = `❌ **Job \`${job}\` failed**`;
            const newBody = comment.body + "\n" + failureMsg;

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: commentId,
              body: newBody
            });




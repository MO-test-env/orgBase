name: Create Submodule PRs (on PR open)

on:
  pull_request:
    types: [opened, reopened, synchronize]

permissions:
  contents: write          # needed to push branches in submodules
  issues: write            # needed to comment on PRs
  pull-requests: write
  actions: read

env:
  CROSS_TOKEN: ${{ secrets.WORKFLOWS }}
  GH_TOKEN: ${{ secrets.WORKFLOWS }}

jobs:
  submodule-prs:
    runs-on: ubuntu-latest
    steps:
      # -----------------------------------------------------------
      # 1️⃣ Checkout base repo
      # -----------------------------------------------------------
      - name: Checkout base repo
        uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0

      # -----------------------------------------------------------
      # 2️⃣ Get changed files for the base PR
      # -----------------------------------------------------------
      - name: Get changed files
        id: changed
        run: |
          gh api \
            repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/files \
            --paginate \
            --jq '.[].filename' > changed_files.txt
        env:
          GH_TOKEN: ${{ env.GH_TOKEN }}

      # -----------------------------------------------------------
      # 3️⃣ Detect changed submodules
      # -----------------------------------------------------------
      - name: Detect changed submodules
        id: detect
        run: |
          echo "[]" > submodules.json

          if [ -f .gitmodules ]; then
            git config --file .gitmodules --get-regexp path | while read key path; do
              url=$(git config --file .gitmodules submodule."$path".url)

              if grep -q "^$path$" changed_files.txt; then
                repo=$(basename "$url" .git)

                tmp=$(mktemp)
                jq --arg path "$path" \
                  --arg url "$url" \
                  --arg repo "$repo" \
                  '. += [{"path": $path, "url": $url, "repo": $repo}]' \
                  submodules.json > "$tmp" && mv "$tmp" submodules.json
              fi
            done
          fi

          # ✅ Use heredoc for multiline JSON
          {
            echo "submodules_json<<EOF"
            cat submodules.json
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
      # -----------------------------------------------------------
      # 4️⃣ Create PRs in changed submodules
      # -----------------------------------------------------------
      - name: Create submodule PRs
        id: create
        if: ${{ steps.detect.outputs.submodules_json != '[]' }}
        run: |
          branch="${{ github.head_ref }}"
          pr_number=${{ github.event.pull_request.number }}

          jq -c '.[]' <<< '${{ steps.detect.outputs.submodules_json }}' | while read sub; do
            path=$(jq -r .path <<< "$sub")
            url=$(jq -r .url <<< "$sub")
            repo=$(jq -r .repo <<< "$sub")

            echo "🔎 Processing $repo at $path"

            # Clone the submodule repo
            git clone "$url" "$repo"
            cd "$repo"

            # Create branch with same name as base PR
            if git ls-remote --exit-code origin "refs/heads/$branch"; then
              echo "Branch $branch already exists in $repo"
              git fetch origin $branch
              git checkout $branch
            else
              echo "Creating new branch $branch in $repo"
              git checkout -b "$branch"
              git push origin "$branch"
            fi

            # Create PR in submodule
            pr_url=$(gh pr create \
              --title "Sync from base PR #$pr_number" \
              --body "This PR was automatically created from ${{ github.repository }}#${pr_number}" \
              --head "$branch" \
              --base main \
              --repo "$url" || true)

            pr_num=null
            if [[ "$pr_url" == https* ]]; then
              pr_num=$(basename "$pr_url")
            fi

            cd ..

            tmp=$(mktemp)
            jq --arg path "$path" \
               --arg url "$url" \
               --arg repo "$repo" \
               --argjson pr_number "$pr_num" \
               '.[] | select(.path==$path) |= . + {"pr_number": $pr_number}' \
               submodules.json > "$tmp" && mv "$tmp" submodules.json
          done

          echo "final_json=$(cat submodules.json)" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ env.GH_TOKEN }}

      # -----------------------------------------------------------
      # 5️⃣ Comment JSON summary back on base PR
      # -----------------------------------------------------------
      - name: Comment JSON on base PR
        if: ${{ steps.create.outputs.final_json != '' }}
        run: |
          gh pr comment ${{ github.event.pull_request.number }} \
            --body "### Submodule PRs\n\`\`\`json\n${{ steps.create.outputs.final_json }}\n\`\`\`"
        env:
          GH_TOKEN: ${{ env.GH_TOKEN }}

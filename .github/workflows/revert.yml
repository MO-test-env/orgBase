name: Auto-Revert Submodules + Base PR

on:
  issue_comment:
    types: [created]

permissions:
  pull-requests: write
  contents: write
  issues: write
  actions: write

env:
  CROSS_TOKEN: ${{ secrets.WORKFLOWS }}

jobs:
  detect-submodules:
    if: |
      github.event.issue.pull_request != null &&
      github.event.comment.body == '/revert' &&
      github.event.comment.user.type != 'Bot'
    runs-on: ubuntu-latest
    outputs:
      submodules: ${{ steps.extract.outputs.submodules }}
    steps:
      - id: extract
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.issue.number;
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber
            });
            const cache = comments.find(c => /<!-- SUBMODULES_JSON -->/.test(c.body));
            if (!cache) { core.setOutput("submodules", "[]"); return; }
            const jsonMatch = cache.body.match(/```json\s*([\s\S]*?)\s*```/);
            core.setOutput('submodules', jsonMatch ? jsonMatch[1] : "[]");

  revert-submodules:
    needs: detect-submodules
    if: needs.detect-submodules.outputs.submodules != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        submod: ${{ fromJson(needs.detect-submodules.outputs.submodules) }}
    steps:
      - uses: actions/github-script@v7
        with:
          github-token: ${{ env.CROSS_TOKEN }}
          script: |
            const sub = ${{ toJson(matrix.submod) }};
            const url = new URL(sub.url);
            const [owner, repo] = url.pathname.replace(/^\/|\.git$/g,'').split('/');
            const prNumber = sub.pr_number;

            // Revert PR via GraphQL
            const revertMutation = `
              mutation($input: RevertPullRequestInput!) {
                revertPullRequest(input: $input) {
                  pullRequest {
                    id
                  }
                }
              }
            `;
            const revertResponse = await github.graphql(revertMutation, {
              input: { pullRequestId: `PRID_${prNumber}` } // PRID is placeholder, will replace
            });
            core.info(`🔄 Requested revert PR for #${prNumber}`);

            // Get the newly created revert PR number via REST
            const { data: pulls } = await github.rest.pulls.list({
              owner,
              repo,
              state: 'open',
              sort: 'created',
              direction: 'desc'
            });
            const revertPR = pulls.find(p => p.title.includes(`Revert "${prNumber}"`));
            if (!revertPR) throw new Error(`❌ Could not find revert PR for #${prNumber}`);

            core.info(`🔢 Revert PR number: ${revertPR.number}`);

            // Merge the revert PR
            await github.rest.pulls.merge({
              owner,
              repo,
              pull_number: revertPR.number,
              merge_method: "squash"
            });
            core.info(`✅ Successfully merged revert PR #${revertPR.number} for original PR #${prNumber}`);

  # revert-base:
  #   needs: revert-submodules
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/github-script@v7
  #       with:
  #         github-token: ${{ env.CROSS_TOKEN }}
  #         script: |
  #           const prNumber = context.issue.number;
  #           const { data: pr } = await github.rest.pulls.get({
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #             pull_number: prNumber
  #           });
  #           if (!pr.merged) return;

  #           const { data: revert } = await github.request("POST /repos/{owner}/{repo}/pulls/{pull_number}/revert", {
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #             pull_number: prNumber
  #           });

  #           await github.rest.pulls.merge({
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #             pull_number: revert.number,
  #             merge_method: "squash"
  #           });

  # bump-submodules:
  #   needs: [revert-submodules, revert-base]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/github-script@v7
  #       with:
  #         github-token: ${{ env.CROSS_TOKEN }}
  #         script: |
  #           const owner = context.repo.owner;
  #           const repo = context.repo.repo;
  #           const workflowFileName = "bump.yml";
  #           const ref = "main";

  #           console.log(`🚀 Dispatching '${workflowFileName}' on ${ref}...`);
  #           await github.rest.actions.createWorkflowDispatch({
  #             owner,
  #             repo,
  #             workflow_id: workflowFileName,
  #             ref
  #           });
  #           console.log("✅ Bump workflow dispatched successfully");
